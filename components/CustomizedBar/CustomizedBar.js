import React from "react";
import { View, StatusBar } from "react-native";

const CustomizedBar = () => {
    return (
        <View>
            <StatusBar />
        </View>
    );
};

export default CustomizedBar;